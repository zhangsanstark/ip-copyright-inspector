笔记排版规则

1）适用范围

本规则适用于仓库内所有 Markdown 笔记、资料索引和说明文件。

2）排版：有编号，不放大字号

- 不使用井号开头的 Markdown 标题。
- 不使用等号或连续连字符放在文字下一行的标题写法。
- 不使用 HTML 的 h1 至 h6 标题标签。
- 不使用粗体、超大字号或全大写英文模拟标题。
- 文件开头可以有一行普通文本名称，不加标题语法。
- 主知识点统一写成 `1）列表怎么用`、`2）字典怎么用`，每篇从 1 连续编号。
- 一个知识点较长时，再用 `2.1 怎样取值`、`2.2 键不存在怎么办` 区分小点，小点编号跟随所在主知识点。
- 编号行仍是普通段落，前后各留一个空行。不要把主知识点写成会自动缩进的 Markdown 有序列表。
- 操作步骤、题号可以使用普通有序列表；它们与知识点编号分开计数。
- 正文使用短段落和普通列表，保持低调、易读。
- 长文在开头放一行简短导航，说明各组知识点的位置，不必堆一大段目录。
- 新增或整理笔记时，提交前检查整份文件是否符合本规则。

3）表达：讲清楚，比听起来专业更重要

- 先用一句话说清“它是干什么的”，再补细节。
- 解释尽量走这条线：要解决什么问题 → 怎么写 → 结果为什么这样。根据内容灵活安排，不把这三句话变成每节重复的标签。
- 优先用小数据举例。Java 对照只在有助于理解差异时补充，不要求每段都类比。
- 比喻要贴合行为，解释完就回到代码；少用“记忆背包”“金库原则”之类需要再解释一次的说法。
- 一个段落只讲一个重点，避免连续堆叠术语。
- 必须使用术语时，紧跟一句通俗解释。
- 示例尽量包含代码、预期输出和常见错误。
- 正文的详细讲解不能用“一句定义加一个例子”代替。遇到第一次出现的函数或难点，要交代：解决什么问题、参数各是什么、实际按什么顺序执行、返回什么，以及常见边界。速记卡和来源索引可以保持简短。
- 先写容易读懂的普通函数或 for 循环，再给 lambda、推导式等简写。不要用还没解释的简写解释另一个新概念。
- 对累计、递归、闭包、装饰器、生成器、异步等有连续步骤的内容，用小输入跟踪每一步的参数、状态或中间结果；仅展示最终输出不算讲清执行过程。
- 例子要由浅到深：最小正常情况 → 改一个条件 → 空输入、错误输入或业务场景。不是每个主题都要堆满案例，但关键差异要能亲手验证。
- 练习给出可核对的结果、提示或参考实现的位置。完整代码需说明运行方式；片段必须说明依赖的上下文，不能把占位函数当作可直接运行的实现。
- 难点可以给一句好记的总结，但不强行为每节编口诀。动手题集中放在适合的位置。
- 语气自然、直接，不使用“先说人话”“你只需要记住”之类反复出现的口头标签。
- 不把“通常如此”写成“永远如此”，重要的例外和适用条件要留下。
- 篇幅以讲透为准，可以按完整书稿组织，但不要用学术化措辞或重复定义填充篇幅。

4）提交前检查

先检查知识点编号是否连续、小点是否归在正确的主点下面，再运行格式检查。改写解释时，也要确认例子、预期输出和关键限制没有被改错。

```powershell
python scripts\check_note_format.py
```

5）普通正文示例：append 和 extend 有什么区别

两者都能往列表末尾加东西，区别在于：把传入的内容当成一个元素，还是把里面的元素逐个加进去。

5.1 append：整体加进去

`items = [1, 2]`，执行 `items.append([3, 4])` 后，结果是 `[1, 2, [3, 4]]`。新列表整体占了一个位置，所以列表长度是 3。

5.2 extend：逐个加进去

同样从 `[1, 2]` 开始，执行 `items.extend([3, 4])` 后，结果是 `[1, 2, 3, 4]`。这次加入了两个元素，所以列表长度是 4。

上面的知识点名称、编号和解释都使用普通正文，不依靠加粗或大字号区分层次。

6）详细手册的内容与验证约定

- `docs/handbook/` 是逐章详细阅读入口，旧专题笔记保留供回看，不把旧内容清空或用一句话替代。
- 每章有简短导航，从普通写法走到简写，解释参数、返回值、状态变化与重要边界；提供至少三道有完整参考答案的练习。
- 所有 Python 代码块必须明确标记：完整且独立的例子用 `# runnable: hb章节号_唯一名称`，依赖上下文的片段用 `# fragment: 说明`，需要选装依赖或外部服务的例子用 `# optional: 前提`。
- runnable 块必须能独立执行，并用有实际意义的 assert 核对结果。不要依赖上一个代码块的变量，不自动连接外部服务或改动真实数据。
- 只出现代码不算讲解完成；正文要说明为什么这样运行。只通过断言也不代表并发压力、生产部署或外部服务已验证。
- 新增内容同时维护手册目录与知识点对照表。只修改一个举例提及的方法，不算完成对整篇深度的修订。
- 运行 `python scripts/check_handbook_examples.py` 校验全部完整例子，运行格式检查及项目测试；明确保留未运行的可选示例边界。
